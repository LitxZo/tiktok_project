Server:
  port: 9999   #运行端口
  rootgroup: /douyin  #路由根地址

Log:
  maxSize: 100 # 日志文件最大尺寸（M）
  maxBackups: 10 # 保留旧文件的最大个数
  maxAge: 10 # 保留旧文件的最大天数
  developMode: true

DB:
  dsn : root:shouwangpigu233@tcp(127.0.0.1:3306)/tiktok_project?charset=utf8mb4&parseTime=True&loc=Local  #这里写自己的数据库用户名、密码和数据库名称
  maxIdleConns : 10 # 最大空闲连接数
  maxOpenConns : 10 # 最大打开连接数
  
Token:
  expiresTime : 300 #token 过期时间（分钟）
  signedKey : tiktok_project.cn # 签名密钥